# LiveKit Agent Framework
livekit-agents[turn-detector]~=1.2
livekit-plugins-openai
livekit-plugins-deepgram
livekit-plugins-elevenlabs
livekit-plugins-silero
livekit-plugins-noise-cancellation

# Environment & Configuration
python-dotenv
certifi

# Web Server (Token Server)
fastapi
uvicorn
python-multipart

# LangChain RAG Framework
langchain
langchain-core
langchain-community
langchain-openai
langchain-chroma
langchain-text-splitters

# Vector Store
chromadb

# Hybrid Search (BM25 keyword search)
rank-bm25

# Re-ranking (CrossEncoder for precise fact retrieval)
# CPU-only PyTorch (no CUDA - App Runner has no GPU, and this is ~200MB vs ~2GB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers

# Document Loaders
pypdf  # Required by langchain's PyPDFLoader
pymupdf
youtube-transcript-api
pytube
yt-dlp
trafilatura
beautifulsoup4
python-docx

# Tools
tavily-python

# Testing
pytest
pytest-asyncio
httpx
livekit  # For client-side WebRTC testing
